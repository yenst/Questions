//-
    IMPORTANT
    When editing -> also paste youe edits in mixin_answer.pug

//-
    This file exists because there is no way to render a mixin via pug.renderFile()
    We need the mixin for the for loop in thread.pug
    TODO Look into switching to EJS instead

//-
    answer.pug variables
    ********************
    @answerObject {Answer} answer to render

li(class="answer list-group-item bg-light" data-answer-id=answerObject._id)
    div(class="row")
        p(class="answerText col")!= answerObject.answer
        button(class="commentButton btn btn-primary text-light col-sm-2 col-md-1" type="submit")
            span(class="fa fa-2x fa-comment" aria-hidden="true")
    ul(class="comments fa-ul")
        each comment in answerObject.comments
            li(class="comment")
                i(class="fa fa-li fa-commenting")
                span!= comment.comment